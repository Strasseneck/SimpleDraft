import React, { FC } from 'react';

interface Props {
  // Define props here
}

const Button: FC<Props> = ({ /* Destructure props here */ }) => {
  // Component logic here

  return (
    <div>
      {/* JSX content here */}
    </div>
  );
};

export default Button;