import React, { FC } from 'react';

const EditorView: FC = () => {
  return (
    <div>
      <p>Editor Working</p>
      {/* JSX content for the view here */}
    </div>
  );
};

export default EditorView;
